#include <stdio.h>

int n, M, m[101], c[101], DT[101][101], ans=987654321;

int min(int a, int b) {return a<b?a:b;}

void solve(int i, int r)
{
    if(r <= 0)
    {
        if()
    }
}

int main()
{
    scanf("%d %d", &n, &M);
    for(int i=0; i<n; i++) scanf("%d", &m[i]);
    for(int i=0; i<n; i++) scanf("%d", &c[i]);
    printf("\n");
    for(int i=0; i<n; i++)
    {
        for(int j=0; j<n; j++) printf("%d ", DT[i][j]);
        printf("\n");
    }
    return 0;
}